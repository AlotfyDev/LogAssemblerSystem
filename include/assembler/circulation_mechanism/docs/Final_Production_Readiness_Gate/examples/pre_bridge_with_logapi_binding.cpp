
#include "pre_bridge_basic_flow.cpp"

/*
    This example reuses the basic PreBridge flow and demonstrates the intended
    injection recipe shape for a future Log API provider plugin adapter.

    The example intentionally does not include Log API runtime. It only proves
    that PreBridge can run independently with a boundary-safe ingress binding.
*/
