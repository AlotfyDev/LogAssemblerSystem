# DEFERRED DECISIONS — CME-ARCH-PACK-06

## 1. Central vs Local Diagnostics

Pending:

```text
central diagnostics/ root
component-local reports only
hybrid local reports + central collector
```

## 2. Report Storage

Pending:

```text
ephemeral report objects
ring buffer
external sink
test-only collection
```

## 3. View Semantics

Pending:

```text
snapshot
live read-only wrapper
copyable DTO
generation-bound view
```

## 4. Severity Model

Pending exact enum names and severity thresholds.

## 5. Exportability

Pending:

```text
internal only
test export
bridge-visible boundary report
diagnostic sink addon
```
