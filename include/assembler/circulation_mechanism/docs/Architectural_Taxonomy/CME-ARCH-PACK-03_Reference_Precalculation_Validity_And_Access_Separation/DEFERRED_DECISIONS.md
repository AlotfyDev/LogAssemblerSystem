# DEFERRED DECISIONS — CME-ARCH-PACK-03

## 1. Reference Object Shape

Pending:

```text
lightweight struct
opaque token
index pair
registry-generation-bound handle
policy-specialized type
```

## 2. TNextRefToken

Pending:

```text
standalone token
view over prepared ref
consumer-facing API rhythm only
```

## 3. Generation Tracking

Pending:

```text
mandatory registry generation and profile generation
optional debug-mode only
or first-class production invariant
```

## 4. Stale Detection Strategy

Pending:

```text
compile-time constrained
runtime checked
diagnostic-only
hybrid
```

## 5. Dispatch Ref Ownership After Exposure

Pending:

```text
Dispatcher retains exposure ownership
Bridge-facing adapter receives transient ref
Bridge-facing adapter receives view only
```
