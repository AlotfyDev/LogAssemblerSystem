# CME-RSCP-W00 — Deferred Decisions

## 1. Bridge Contract

Deferred:

```text
copy / move / borrow semantics
bridge acknowledgement
bridge failure classification
bridge observation requirement
```

## 2. Receiver / Add-on Contract

Deferred:

```text
receiver acknowledgement
receiver retry acceptance
receiver backpressure contract
persistence/addon completion semantics
```

## 3. Reuse vs Specialization

Deferred:

```text
reuse pre-bridge containers/registry/rounds directly
or specialize read-side profiles and types
```

## 4. Persistence

Deferred:

```text
persistence as add-on boundary
persistence deletion as downstream concern
read-side diagnostic only
```
