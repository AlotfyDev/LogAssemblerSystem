#pragma once

#include "../../base/TBoundedRegistry.hpp"

/**
 * @file TBoundedRegistry.hpp
 * @brief Compatibility forwarding header for the canonical bounded registry primitive.
 *
 * @section ascc_comp_w01_registry_unification Registry Unification
 * `optional_registries/base` is now a historical compatibility path.  The
 * canonical bounded registry primitive lives at:
 *
 * `registries/base/TBoundedRegistry.hpp`
 *
 * This header intentionally defines no duplicate type.
 */
