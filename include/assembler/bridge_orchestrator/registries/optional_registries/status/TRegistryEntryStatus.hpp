#pragma once

#include "../../status/TRegistryEntryStatus.hpp"

/**
 * @file TRegistryEntryStatus.hpp
 * @brief Compatibility forwarding header for the canonical registry entry status.
 *
 * @section ascc_comp_w01_registry_unification Registry Unification
 * `optional_registries/status` is now a historical compatibility path.  The
 * canonical registry status surface lives at:
 *
 * `registries/status/TRegistryEntryStatus.hpp`
 *
 * This header intentionally defines no duplicate type.
 */
